function est = estimation(S,step,conf);
%ESTIMATION The best guess for the state of the system - estimated value
%CMP Vision Algorithms http://visionbook.felk.cvut.cz
%
% Compute the best guess for the state of the system based on the
% .
% 
% Usage: est = estimation(S,conf)
%   S  Structure with samples and weights.
%   conf  Structure with configuration.
% Outputs:
%   est  Value of the predicted state.
%
%

% Not implemented for this experiment
est = 0;
